# Requirements for F1 Hybrid Prediction System - Incremental Trainer
# Install with: pip install -r requirements_incremental.txt

# Core dependencies
watchdog>=3.0.0          # For folder monitoring and auto-retraining
pathlib2>=2.3.7          # Enhanced path operations (for older Python versions)

# Optional: If you need enhanced pathlib support on older Python versions
# pathlib2>=2.3.7; python_version<"3.4"

# Note: The following are already part of your existing F1 prediction system:
# - numpy, pandas, scikit-learn (for ML layer)
# - requests (for API calls)
# - json, logging, datetime, hashlib (built-in Python modules)
